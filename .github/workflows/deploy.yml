name: Deploy to server

on:
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "Hola mundo desde mi primer flujo de git action"

  second-job:
    needs: first-job
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo segundo
        run: echo "Hola mundo desde mi segundo flujo de git action"