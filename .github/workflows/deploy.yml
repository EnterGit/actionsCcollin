name: Deploy to server

on:
  - push
jobs:
  first-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Hola mundo
        run: echo "Hola mundo desde mi primer flujo de git action"
        