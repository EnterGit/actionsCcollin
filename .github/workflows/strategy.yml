name: Deploy to server

on : push

jobs:
  first:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        message: ["hola","adios","chau"]
    steps:
      - name: Obtener mensaje
        run: "echo ${{ matrix.message }}"
         
